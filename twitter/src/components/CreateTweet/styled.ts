import { styled } from "styled-components";

export const TweetTextArea = styled.textarea`
  width: 100%;
  height: auto;
  max-height: 300px;

  border: none;
  font-family: "Roboto";
  font-style: normal;
  font-weight: 600;
  font-size: 22px;
  line-height: 26px;

  color: #828282;
  resize: none;
`;
